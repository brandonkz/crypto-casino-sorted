{
  "name": "crypto-casino-wallet-tracker",
  "version": "1.0.0",
  "description": "On-chain gambling wallet analytics",
  "main": "fetch-wallet-data.js",
  "scripts": {
    "fetch": "node fetch-wallet-data.js",
    "update": "node fetch-wallet-data.js && git add gambling-data.json && git commit -m 'Update wallet data' && git push",
    "verify:offers": "node verify-offers.js",
    "verify:offers:update": "node verify-offers.js --update-baseline"
  },
  "dependencies": {
    "agent-base": "^8.0.0",
    "dotenv": "^16.6.1",
    "googleapis": "^171.4.0",
    "nodemailer": "^8.0.4"
  }
}
